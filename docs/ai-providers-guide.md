# 国内免费大模型 AI 报告生成指南

## 推荐方案：DeepSeek (完全免费)

### 1. 获取 DeepSeek API Key

访问：https://platform.deepseek.com/

1. 注册账号（支持国内手机号）
2. 进入控制台
3. 创建 API Key
4. 复制 API Key（格式：`sk-...`）

**优势**:
- ✅ 完全免费（每天500万 tokens 额度）
- ✅ 国内访问速度快
- ✅ 兼容 OpenAI API 格式
- ✅ 性能优秀，适合代码分析

### 2. 配置环境变量

编辑 `.env` 文件，添加：

```bash
# DeepSeek API 配置 (推荐)
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here
```

### 3. 安装依赖

```bash
./run.sh pip install openai
```

### 4. 使用方法

#### 生成完整 AI 报告
```bash
./run.sh python scripts/generate_universal_ai_report.py --provider deepseek
```

#### 仅生成摘要
```bash
./run.sh python scripts/generate_universal_ai_report.py --provider deepseek --summary-only
```

#### 自定义输出路径
```bash
./run.sh python scripts/generate_universal_ai_report.py \
  --provider deepseek \
  --output reports/my-report.md
```

---

## 备选方案：豆包 (字节跳动)

### 1. 获取豆包 API Key

访问：https://console.volcengine.com/ark

1. 注册火山引擎账号
2. 开通豆包服务
3. 创建 API Key

### 2. 配置环境变量

```bash
# 豆包 API 配置
DOUBAO_API_KEY=your-doubao-api-key
DOUBAO_API_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
```

---

## 对比选择

| 特性 | DeepSeek | 豆包 | Claude |
|------|---------|------|--------|
| **费用** | ✅ 完全免费 | 💰 付费(有免费额度) | 💰💰 付费 |
| **国内访问** | ✅ 快速 | ✅ 快速 | ❌ 需要代理 |
| **API 格式** | OpenAI 兼容 | 自定义 | Anthropic |
| **性能** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **代码分析** | ✅ 优秀 | ✅ 良好 | ✅ 优秀 |
| **免费额度** | 500万 tokens/天 | 10万 tokens | 无免费 |

**推荐**: 使用 **DeepSeek**，完全免费且性能优秀！

---

## 示例输出

### 测试统计
- 总测试数: 27
- 通过: 21
- 失败: 4
- 通过率: 77.78%

### AI 生成的报告包括:
1. ✅ 执行摘要
2. 📊 关键指标表格
3. 🔍 失败原因深度分析
4. 📈 趋势洞察
5. 💡 修复建议

---

## 故障排除

### 问题：API Key 无效
检查 `.env` 文件中的 API Key 是否正确配置

### 问题：网络连接错误
DeepSeek 服务器在国内，不需要代理

### 问题：依赖缺失
```bash
./run.sh pip install openai python-dotenv
```

---

## 成本对比

**DeepSeek**:
- 每天生成报告次数：100+ 次
- 月度成本：¥0 (完全免费)

**Claude**:
- 每次报告约 ¥0.5-1
- 月度成本：¥50-100

**推荐使用 DeepSeek 节省成本！**
